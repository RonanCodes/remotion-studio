node_modules/
out/
dist/
.DS_Store

# Local overrides (not committed)
*.local.*
*.local/

# Playwright MCP (browser automation local state)
.playwright-mcp/

# Claude Code runtime state
.claude/scheduled_tasks.lock

# Environment
.env
.env.local
.env.*.local

# Project repos are cloned into src/projects/ individually.
# Bundled projects (showcase) and the README are tracked.
src/projects/*
!src/projects/README.md
!src/projects/showcase
